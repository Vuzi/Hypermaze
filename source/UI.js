
/**
 * UI class, that handles the construction and events in the UI. Will allow to
 * launch the game according to the given parameters.
 * @param {Map} map                   The map to use.
 * @param {Graph} graph               The graph to use.
 * @param {Pawn_handler} pawn_handler The pawn handler.
 * @param {Stat} stat                 The stat handler, can be null if no stats are needed.
 */
UI = function(map, graph, pawn_handler, stat) {
	this.map = map;
	this.graph = graph;
	this.pawn_handler = pawn_handler;
	this.stat = 
}

/**
 * Init the UI events 
 * @param  {[type]} view [description]
 * @return {[type]}      [description]
 */
UI.prototype.init = function(view) {
	// body...
};